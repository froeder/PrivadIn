# Configuração de Banco Firestore para o Mobile PrivadIn
# Por padrão em desenvolvimento (IS_DEV = true) conecta em "dev-privadin".
# Em produção conecta em "(default)".
# Se desejar conectar no banco de produção durante o desenvolvimento, descomente abaixo:
# EXPO_PUBLIC_FIRESTORE_DATABASE_ID=(default)
